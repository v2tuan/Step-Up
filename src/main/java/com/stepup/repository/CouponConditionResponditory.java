package com.stepup.repository;

import com.stepup.entity.CouponCondition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CouponConditionResponditory extends JpaRepository<CouponCondition, Long> {
}
